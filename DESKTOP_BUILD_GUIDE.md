# 🖥️ Changsoo's 방송 편집 도우미 윈도우 프로그램(.exe) 빌드 및 실행 가이드

이 웹 애플리케이션을 바탕으로 **독립된 윈도우 전용 설치 없이 실행되는 단일 파일(.exe)**로 제작할 수 있는 완벽한 환경을 구성해 두었습니다. 

이 프로젝트는 초경량 웹 앱 프레임워크인 **Electron**과 **Electron-Builder**를 이용해 패키징됩니다. 아래 가이드를 따라서 단 한번의 명령어로 윈도우용 완성본 프로그램을 만들 수 있습니다.

---

## 📌 사전 준비 사항

윈도우 PC에 아래의 도구들이 설치되어 있어야 합니다:

1. **Node.js 설치**
   * 공식 홈페이지([https://nodejs.org/](https://nodejs.org/))에서 안정화 버전(LTS)을 다운로드하여 설치해 주세요.
2. **이 프로젝트 다운로드**
   * AI Studio 오른쪽 상단의 환경 설정 아이콘(⚙️) 메뉴에서 **Export to ZIP**으로 다운로드하여 압축을 해제하거나, **GitHub**로 동기화해서 소스 코드를 컴퓨터로 가져옵니다.

---

## 🚀 윈도우 프로그램 빌드 및 실행 방법

명령 프롬프트(CMD) 또는 터미널을 열고 압축을 해제한 디렉터리로 이동한 후, 아래 순서대로 명령어를 입력해 주세요.

### 1단계. 관련 라이브러리 설치
이 프로젝트에 추가된 Electron 관련 필수 개발 모듈들을 설치합니다.
```bash
npm install
```

### 2단계. 로컬 개발 환경에서 앱 실행 (선택 사항)
빌드하기 전에 제대로 실행되는지 데스크톱용 프로그램 미리보기로 테스트해 보실 수 있습니다.
```bash
# 먼저 Vite 개발 서버를 백그라운드에 켭니다.
npm run dev

# 다른 터미널 창을 열고, 데스크톱 전용 윈도우 창으로 실행합니다.
npm run electron:dev
```

### 3단계. 윈도우용 단일 실행 파일(.exe)로 빌드하기
모든 리소스를 정적 컴파일한 뒤, 설치 과정 없이 더블 클릭으로 바로 작동하는 **무설치 포터블형 `.exe` 단일 파일**을 완성하는 명령어입니다.
```bash
npm run electron:build
```

---

## 📂 최종 빌드 결과물 경로

빌드가 오류 없이 성공적으로 완료되면 프로젝트 폴더 내에 **`dist-electron/`** 이라는 새로운 폴더가 생성됩니다.

* **최종 파일 예시**: `dist-electron/Changsoo's 방송 편집 도우미 1.0.0.exe` (또는 지정된 이름의 `.exe` 파일)
* 이 파일만 USB나 다른 컴퓨터로 복사해서 전달하면 설치 과정 없이 즉시 윈도우 데스크톱 프로그램으로 오디오 변환과 정밀 편집을 활용하실 수 있습니다!

---

## 🛡️ "PC 보호" (Windows SmartScreen) 경고창 해결 방법

직접 컴파일한 프로그램 실행 시 **"Windows의 PC 보호"** 파란색 화면(SmartScreen 경고)이 나타나는 것은 **매우 정상적인 현상**입니다.

### 💡 왜 안전한데 이 경고가 뜨나요?
* 마이크로소프트(MS)는 개발자가 수백만 원 상당의 비용을 지불하고 본인 인증을 거쳐 발급받는 **'코드 서명 인증서(Code Signing Certificate)'**가 서명되지 않은 모든 가내 수공업 사설 프로그램(.exe)에 대해 "실제 제작한 사용자가 누군지 알 수 없다"는 취지에서 기본적으로 경고창을 띄웁니다.
* 직접 소스 코드를 다운로드하여 내 컴퓨터에서 빌드한 것이므로 **애드웨어나 바이러스가 일절 없는 100% 안전한 프로그램**입니다. 걱정하지 마시고 아래 절차대로 실행해 주세요.

### 🚀 실행 경로 개방 순서
1. 파란색 경고 패널이 나타나면 문구 중간의 **`추가 정보`** 라는 작고 얇은 밑줄 글씨를 클릭합니다.
2. 클릭하면 숨겨져 있던 **`앱: Changsoo's 방송 편집 도우미`**, **`게시자: 알 수 없는 게시자`** 정보가 표시됩니다.
3. 이와 동시에 우측 하단에 **`실행`** 버튼이 나타납니다.
4. **`실행`** 버튼을 클릭하시면 정상적으로 백그라운드나 오류 없이 데스크톱 앱이 활성화됩니다.
5. (한 번만 이를 허용하여 실행하면 다음 실행부터는 경고창 없이 더블 클릭만으로 바로 켜지게 됩니다.)

---

## 🌟 데스크톱 프로그램 적용 시 얻는 이점

1. **완벽한 로컬 환경 및 속도**
   * 브라우저 프레임 밖으로 요소가 튀어나오거나 이탈하는 UI 오차가 전혀 없으며, 윈도우 표준 해상도로 완벽히 고정되어 편안하게 편집하실 수 있습니다.
2. **독립된 환경**
   * 크롬이나 브라우저 환경에 구애받지 않고 마치 정밀한 오디오 마스터링 윈도우 프로그램처럼 전용 창으로 깔끔하게 동작합니다.
3. **오디오 압축 전사**
   * 인메모리 상에서 제공되는 고속 오디오 디코더가 브라우저 API 호출과 완전히 연계되어 대용량 WAV 오디오도 순식간에 모노 16kHz 가벼운 포맷으로 전환하여 API에 전달하므로 반응 속도가 극대화됩니다.
